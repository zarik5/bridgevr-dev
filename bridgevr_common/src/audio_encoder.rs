use crate::settings::AudioEncoderDesc;

pub struct AudioEncoder {}

impl AudioEncoder {
    pub fn new(settings: AudioEncoderDesc) -> Self {
        panic!();
    }

    pub fn encode(&mut self, sample: &[u8]) {
        
    }
}
